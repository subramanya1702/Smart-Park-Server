/**
 * A constant object to define the supported image types
 * @type {{ORIGINAL_IMAGE: string, LABELLED_IMAGE: string}}
 */
const imageType = {
    ORIGINAL_IMAGE: 'originalImage',
    LABELLED_IMAGE: 'labelledImage'
};

module.exports = imageType;
